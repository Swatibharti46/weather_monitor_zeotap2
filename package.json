{
  "name": "json-server-starter",
  "private": true,
  "scripts": {
    "frontend": "http-server",
    "backend": "json-server -w db.json"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "chart.js": "^4.4.5",
    "http-server": "^0.12.3",
    "nodemon": "^3.1.7"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  },
  "description": "1. Run `npm run backend` in the terminal to start the server. 2. Open a second terminal (by clicking the `[+]` icon) and run `npm run frontend` to open a simple page that fetches data from our backend",
  "version": "1.0.0",
  "main": "index.js",
  "author": "swati",
  "license": "ISC"
}
